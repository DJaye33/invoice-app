const AppBar = () => {
  return (
    <header className="flex bg-indigo-950">
      <div className="bg-violet-600">
        <img src={"/logo.svg"} alt="invoice app" />
      </div>
    </header>
  );
};

export default AppBar;
